// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        "build": false,
        "node_modules": true,
        ".wrangler": true
    }
}